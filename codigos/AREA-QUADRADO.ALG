Algoritmo "Area-quadrado"
//  Faça um algoritmo em que o operador informando o lado de um
//  quadrado obtenha como resultado a área do mesmo
Var
   lado, area: real
    
Inicio
      escreva("Olá, digite o valor do tamanho do lado do quadrado: ")
      leia(lado)
      area <- lado^2
      escreval("A área total do quadrado é:", area)
      
Fimalgoritmo